sh_binary(
    name = "audit",
    srcs = ["audit.sh"],
    args = ["$(rootpath //x/crates:Cargo.lock)"],
    data = [
        "@//x/crates:Cargo.lock",
        "@crates//:cargo-audit__cargo-audit",
    ],
    env = {
        "CARGO_AUDIT_BIN": "$(rootpath @crates//:cargo-audit__cargo-audit)",
    },
)
