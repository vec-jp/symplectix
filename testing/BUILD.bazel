load("@rules_rust//rust:defs.bzl", "rust_library", "rust_test")

rust_library(
    name = "testing",
    testonly = True,
    srcs = glob(["*.rs"]),
    visibility = ["//visibility:public"],
    deps = [
        "@lib_crates//:once_cell",
        "@lib_crates//:tempfile",
        "@rules_rust//tools/runfiles",
    ],
)

rust_test(
    name = "testing_unit_test",
    crate = ":testing",
)
