load("@rules_rust//rust:defs.bzl", "rust_library")

package(default_visibility = ["//:__subpackages__"])

rust_library(
    name = "testing",
    testonly = True,
    srcs = glob(["*.rs"]),
    deps = [
        "@crates//:once_cell",
        "@crates//:tempfile",
    ],
)
