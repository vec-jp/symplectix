sh_binary(
    name = "audit",
    srcs = ["audit.sh"],
    args = [
        "$(location //thirdparty/crates:cargo.lock)",
    ],
    data = [
        "//thirdparty/crates:cargo.lock",
        "@crates//:cargo-audit__cargo-audit",
    ],
    env = {
        "CARGO_AUDIT_BIN": "$(location @crates//:cargo-audit__cargo-audit)",
    },
)
