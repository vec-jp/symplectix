load("@rules_rust//rust:defs.bzl", "rust_binary")

package(default_visibility = ["//visibility:public"])

exports_files([
    "BUILD.libssh2.bazel",
])

rust_binary(
    name = "agent",
    srcs = ["agent.rs"],
    deps = ["@crates//:ssh2"],
)
