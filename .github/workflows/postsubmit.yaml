name: Postsubmit

on:
  push:
    branches:
      - main

jobs:
  testing:
    name: Testing
    strategy:
      matrix:
        os: [ubuntu-22.04, macos-12]
    uses: ./.github/workflows/testing.yaml
    with:
      os: ${{ matrix.os }}
