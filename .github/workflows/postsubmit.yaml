name: postsubmit

on:
  push:
    branches:
      - main

jobs:
  presubmit:
    strategy:
      matrix:
        os: [ubuntu-22.04, macos-12]
    uses: ./.github/workflows/presubmit.yaml
    with:
      os: ${{ matrix.os }}
