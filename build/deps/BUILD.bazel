load("@bazel_skylib//rules:build_test.bzl", "build_test")

build_test(
    name = "build_test",
    targets = [
        "@libgit2//:libgit2",
        "@libssh2//:libssh2",
        "@openssl//:openssl",
        "@zlib//:zlib",
    ],
)
