load("@rules_rust//rust:defs.bzl", "rust_binary")

rust_binary(
    name = "semantic_test",
    srcs = ["main.rs"],
    deps = [
        "@lib_crates//:tree-sitter",
        "@lib_crates//:tree-sitter-rust",
    ],
)
